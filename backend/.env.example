# Visage IQ — Backend Environment Configuration

# Service host & port configuration
HOST=0.0.0.0
PORT=8000

# Allowed CORS Origins (comma-separated or '*' for dev)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Local Ollama Multimodal Vision Model & Daemon URL
OLLAMA_MODEL=llava-phi3:latest
OLLAMA_HOST=http://localhost:11434
